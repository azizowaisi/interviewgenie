apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: interview-ai
  labels:
    app: llm-service
spec:
  selector:
    app: llm-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
