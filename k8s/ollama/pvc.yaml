apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: interview-ai
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # storageClassName: optional, use default
