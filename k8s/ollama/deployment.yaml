apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  namespace: interview-ai
  labels:
    app: ollama
    model: mistral
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama
  template:
    metadata:
      labels:
        app: ollama
    spec:
      # Remove nodeSelector if you're on amd64
      # nodeSelector:
      #   kubernetes.io/arch: arm64
      initContainers:
        - name: ensure-ollama-dirs
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              set -e
              mkdir -p /root/.ollama/models
              chmod 755 /root/.ollama /root/.ollama/models
          volumeMounts:
            - name: models
              mountPath: /root/.ollama
        - name: pull-mistral-model
          image: ollama/ollama:latest
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              set -eu
              ollama serve >/tmp/ollama-init.log 2>&1 &
              pid="$!"
              cleanup() {
                kill "$pid" >/dev/null 2>&1 || true
              }
              trap cleanup EXIT
              i=0
              until ollama list >/dev/null 2>&1; do
                i=$((i + 1))
                if [ "$i" -gt 60 ]; then
                  echo "Ollama daemon did not become ready in time"
                  exit 1
                fi
                sleep 2
              done
              ollama pull mistral
              ollama list | grep -q "mistral"
          env:
            - name: OLLAMA_HOST
              value: "127.0.0.1:11434"
            - name: OLLAMA_MODELS
              value: /root/.ollama/models
          volumeMounts:
            - name: models
              mountPath: /root/.ollama
      containers:
        - name: ollama
          image: ollama/ollama:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 11434
          env:
            - name: OLLAMA_HOST
              value: "0.0.0.0"
            - name: OLLAMA_MODELS
              value: /root/.ollama/models
          volumeMounts:
            - name: models
              mountPath: /root/.ollama
          resources:
            requests:
              memory: "8Gi"
              cpu: "2000m"
            limits:
              memory: "16Gi"
              cpu: "4000m"
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - ollama list | grep -q "mistral"
            initialDelaySeconds: 20
            periodSeconds: 5
            failureThreshold: 6
          startupProbe:
            exec:
              command:
                - sh
                - -c
                - ollama list | grep -q "mistral"
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 90
          livenessProbe:
            tcpSocket:
              port: 11434
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: ollama-models
