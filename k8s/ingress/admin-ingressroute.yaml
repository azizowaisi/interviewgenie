# Operations UI: https://admin.interviewgenie.example.com → web (/admin).
# DNS: A record admin → same IP as main site (e.g. 203.0.113.10).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: interview-ai-admin
  namespace: interview-ai
spec:
  entryPoints:
    - web
    - websecure
  tls:
    certResolver: le
    domains:
      - main: admin.interviewgenie.example.com
  routes:
    - match: Host(`admin.interviewgenie.example.com`)
      kind: Rule
      services:
        - name: web
          port: 3002
          nativeLB: true
