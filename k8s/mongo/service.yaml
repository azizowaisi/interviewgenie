apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: interview-ai
  labels:
    app: mongo
spec:
  ports:
    - port: 27017
      targetPort: 27017
      name: mongo
  selector:
    app: mongo
  clusterIP: None   # headless for StatefulSet
