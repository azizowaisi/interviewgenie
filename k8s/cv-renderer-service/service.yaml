apiVersion: v1
kind: Service
metadata:
  name: cv-renderer-service
  namespace: interview-ai
  labels:
    app: cv-renderer-service
spec:
  selector:
    app: cv-renderer-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
