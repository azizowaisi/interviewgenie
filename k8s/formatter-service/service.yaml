apiVersion: v1
kind: Service
metadata:
  name: formatter-service
  namespace: interview-ai
  labels:
    app: formatter-service
spec:
  selector:
    app: formatter-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
