apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: interview-ai
  labels:
    app: monitoring-service
spec:
  selector:
    app: monitoring-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
